# TODOs

Items that don't belong to any single step's plan in `docs/plan.md`. Each step's "Decisions for this step" subsection in plan.md is authoritative for that step.

- **Migrations must be backward-compatible** (convention applying to every step with a schema change). Add-column: OK. Drop-column: requires expand-contract over two deploys (deploy code that no longer reads the column → deploy migration that drops it). Rename: same shape. Documented in `docs/self-host.md` (step 12) and referenced from every migration file's header comment. Effort: N/A (convention). Priority: P1 (immediate, applies starting step 3).

- **MV upgrade choreography runbook.** When evolving a materialized view (e.g. `events_v2 → events_v3`) while data is live: spec the parallel-MV + backfill + read-swap + drop procedure. Write the runbook the first time we need it, not preemptively. Effort: M when needed. Priority: P3.

- **Member removal from team.** Step 4 ships team invites + member list, but no way to undo membership. A team grows monotonically until an operator runs SQL. Add `DELETE /teams/:id/members/:user_id` + a "Remove" action on the member row. Open question for the implementer: does the kicked user's session need to be invalidated for that team's projects (sessions are user-scoped, not team-scoped, so just removing the membership row is enough — the next `viewer.Projects().ByID(p)` returns `ErrNotVisible`). No "last-admin" guard needed since there's no role model in v1, but consider preventing removal of the team's last remaining member to avoid orphaning the team. Defer until a self-hoster reports the gap. Effort: S. Priority: P3.

- **OAuth token revocation cascade on team-membership removal.** Sibling of the "Member removal from team" entry above. When a team_memberships DELETE lands, the same tx should `UPDATE oauth_access_tokens SET revoked_at = NOW() WHERE user_id = $1 AND project_id IN (SELECT id FROM projects WHERE team_id = $2)`. Currently a 1-hour exposure window via TTL is acceptable but not ideal. Bundle with the member-removal work, not separately. Effort: S. Priority: P3.

- **Background cleanup of expired oauth_codes + oauth_access_tokens.** Tables grow monotonically. The same problem exists for `sessions` (also no cleanup). Defer until first prod traffic or operator complaint; bundle into a separate `cmd/maintenance` binary run on host-cron / Kamal-scheduled-task so the main server binary stays single-purpose. Don't introduce a goroutine-cron in the main process for one job. Effort: S. Priority: P3.

- **`/oauth/revoke` endpoint (RFC 7009).** Not required by MCP spec; standard for "log out / disconnect" flows. ~20 LOC: parse form, hash bearer, `UPDATE oauth_access_tokens SET revoked_at = NOW() WHERE token_hash = $1 AND revoked_at IS NULL`. RFC 7009 §2.2 requires returning 200 even for unknown tokens (anti-enumeration). Build when a real client asks (CLI `mere logout`, MCP "Disconnect"). Effort: S. Priority: P3.

- **Connected-apps page (`/account/connected-apps`).** Self-service UI listing the user's active OAuth grants (client name, project name, created_at, last_used_at) with per-row revoke buttons. Needs: a new `last_used_at TIMESTAMPTZ` column on `oauth_access_tokens` updated by `RequireBearer` (single-row UPDATE, cheap); the existing `ListActiveAccessTokensForUser` query; the page + handler; a revoke action (inline POST handler OR reused `/oauth/revoke`). Build alongside or shortly after `/v1/query` + `/mcp` land — the page is most useful once real clients are authorizing. Effort: M (~200 LOC). Priority: P2.

- **Revoked-tokens audit view.** Step 4's `/projects/:id/tokens` listed active tokens only and was removed in step 5 (no per-project secret-token UI anymore). If a future surface lists access tokens, the same gap applies: once revoked a token disappears from the UI. Add a "Revoked" tab on the connected-apps page once it lands. Effort: S. Priority: P3.
