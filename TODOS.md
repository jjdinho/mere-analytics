# TODOs

Items that don't belong to any single step's plan in `docs/plan.md`. Each step's "Decisions for this step" subsection in plan.md is authoritative for that step.

- **Migrations must be backward-compatible** (convention applying to every step with a schema change). Add-column: OK. Drop-column: requires expand-contract over two deploys (deploy code that no longer reads the column → deploy migration that drops it). Rename: same shape. Documented in `docs/self-host.md` (step 10) and referenced from every migration file's header comment. Effort: N/A (convention). Priority: P1 (immediate, applies starting step 3).

- **MV upgrade choreography runbook.** If we later add materialized views while data is live: spec the parallel-MV + backfill + read-swap + drop procedure. Write the runbook the first time we need it, not preemptively. Effort: M when needed. Priority: P3.

- **Member removal from team.** Step 4 ships team invites + member list, but no way to undo membership. A team grows monotonically until an operator runs SQL. Add `DELETE /teams/:id/members/:user_id` + a "Remove" action on the member row. Open question for the implementer: does the kicked user's session need to be invalidated for that team's projects (sessions are user-scoped, not team-scoped, so just removing the membership row is enough — the next `viewer.Projects().ByID(p)` returns `ErrNotVisible`). No "last-admin" guard needed since there's no role model in v1, but consider preventing removal of the team's last remaining member to avoid orphaning the team. Defer until a self-hoster reports the gap. Effort: S. Priority: P3.

- **OAuth token revocation cascade on team-membership removal.** Sibling of the "Member removal from team" entry above. When a team_memberships DELETE lands, the same tx should `UPDATE oauth_access_tokens SET revoked_at = NOW() WHERE user_id = $1 AND project_id IN (SELECT id FROM projects WHERE team_id = $2)`. Currently a 1-hour exposure window via TTL is acceptable but not ideal. Bundle with the member-removal work, not separately. Effort: S. Priority: P3.

- **Background cleanup of expired oauth_codes + oauth_access_tokens [DONE].** Shipped as `cmd/maintenance` + `internal/maintenance`. One-shot binary baked into the Docker image alongside `mere-server`; sweeps `oauth_codes`, `oauth_access_tokens`, and `sessions` rows whose `expires_at < NOW()`. Revoked-but-unexpired access tokens are deliberately preserved (1h TTL drains them naturally; leaves room for the future revoked-tokens audit view). `oauth_clients` is never touched. Invocation is on the operator: host cron or Kamal scheduled-task on a cadence of their choice — Step 8's `config/deploy.yml` should wire a default alias when it lands. No `--dry-run` (sweep is idempotent and bounded by `NOW()`).

- **`/oauth/revoke` endpoint (RFC 7009).** Not required by MCP spec; standard for "log out / disconnect" flows. ~20 LOC: parse form, hash bearer, `UPDATE oauth_access_tokens SET revoked_at = NOW() WHERE token_hash = $1 AND revoked_at IS NULL`. RFC 7009 §2.2 requires returning 200 even for unknown tokens (anti-enumeration). Build when a real client asks (CLI `mere logout`, MCP "Disconnect"). Effort: S. Priority: P3.

- **Connected-apps page (`/account/connected-apps`).** Self-service UI listing the user's active OAuth grants (client name, project name, created_at, last_used_at) with per-row revoke buttons. Needs: the existing `ListActiveAccessTokensForUser` query; the page + handler; a revoke action (inline POST handler OR reused `/oauth/revoke`). Build alongside or shortly after project-scoped query + `/mcp` land — the page is most useful once real clients are authorizing. Effort: M (~200 LOC). Priority: P2.

- **Revoked-tokens audit view.** Step 4's `/projects/:id/tokens` listed active tokens only and was removed in step 5 (no per-project secret-token UI anymore). If a future surface lists access tokens, the same gap applies: once revoked a token disappears from the UI. Add a "Revoked" tab on the connected-apps page once it lands. Effort: S. Priority: P3.

- **`cmd/maintenance` extension to sweep quarantined `failed_events`.** Hard-delete `failed_events` rows where `quarantined_at IS NOT NULL AND quarantined_at < NOW() - INTERVAL '30 days'`. Quarantined rows are accepted-data-loss markers; without a sweeper they accumulate forever and inflate index sizes / backup footprint. Mirrors the `cmd/maintenance` pattern landed in PR #11; cheap (single DELETE on indexed condition). Trade-off: forensic data lost after 30 days. Effort: S. Priority: P3. Depends on Step 5 landing (this PR).

- **ClickHouse dedup key for client-retry safety.** Add a `dedup_key` column to `events_raw_v1` (or switch to a retry-safe table engine) to make ingest retries safe at the CH layer. Step 5's atomic batch enqueue prevents server-side partial duplication, but a client retrying an entire batch after CH already wrote it (network blip between commit and 202) lands duplicates. No exactly-once guarantee end-to-end. Pros: retry-safe SDKs; simplifies client error handling; future-proofs precise counters. Cons: CH schema migration (heavier than PG); possible engine swap to `ReplacingMergeTree` (background merges, eventual-only dedup); SDK changes for `dedup_key` generation. OK for analytics; will bite when exposing precise counters. Effort: M. Priority: P2. Depends on SDK retry-semantics design.

- **Per-project ingest rate limit.** Per-project token-bucket or sliding-window limiter on `POST /ingest/v1/events`. Public token = anyone can spam; the 503-on-saturation in Step 5 is process-wide backpressure, so a single misbehaving snippet can drown other projects. Pros: multi-tenant fairness; protection from accidental SDK loops; mitigates leaked-token abuse. Cons: state management (Redis vs. in-memory vs. PG); operationally non-trivial; cost for low-traffic projects. Relevant as soon as multiple customer projects share an instance. Effort: M. Priority: P2. Depends on rate-limit infra decision + per-project config storage. **Design:** the in-core piece is the no-op `RateLimiter` seam — see `docs/plans/ratelimiter-seam.md` (interface + honored call sites, default allow-all); **this seam has landed** (`extension.RateLimiter`, wired on the ingest/query/MCP chains). The actual per-project limiter (Redis token bucket / plan-aware quotas) is supplied by the hosted wrapper, per `docs/adr/0002-open-core-hosted-wrapper.md`; a self-hoster can also drop in their own.
