# Build artifacts
/bin/
/out/
/dist/
mere-server
mere-maintenance
/server
/maintenance

# Test artifacts
*.test
*.out
coverage.txt

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local env / secrets
.env
.env.local
.kamal/secrets

# Operator-local Kamal config (copied from config/deploy.example.yml and
# filled in per deployment; never committed). The template is committed.
/config/deploy.yml

# Docker volumes (in case anyone runs compose with default volume paths)
/data/

# Local Claude Code instructions (not committed)
/CLAUDE.md
